#ifndef UTIL_H
#define UTIL_H

#define DEFAULT_LENGTH 10

int op_to_pres(char);
int is_val(char);
int is_op(char);
int is_alpha(char);
void print_val(int, int, double, int);
void clear_line(int);

#endif
